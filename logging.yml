version: 1
disable_existing_loggers: True
formatters:
  simple:
    format: '%(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    level: ERROR
    stream: ext://sys.stdout
root:
  level: ERROR
  handlers: [console]
